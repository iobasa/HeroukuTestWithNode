const config = {
    uname: 'root',
    upass: 'root', //windows users leave this blank
    port: 8889, //windows 3306
    host: 'localhost',
    db: 'bizcard'
}

// make this public file our app can read
module.exports = config;